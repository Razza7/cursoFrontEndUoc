const colores=['orange','black','red','green','blue'];
  //let pos= colores.indexOf('blue');
  //console.log('pos:'+ pos);
  //colores.splice(pos,1);
  //console.dir(colores);

  //while, do while, for iteradores
  // forEach devuelve todos los valores del array

  //colores.forEach(function(color){
  //console.log(color);

  //})